<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table{
            border-collapse: collapse;
            width: 400px;
            height: 200px;
        }
        th,td{
            border: 1px black solid;
            font-size: 26px;
        }
        td{
            text-align: center;
            font-size: 20px;
        }
    </style>
</head>
<body>
    <table>
            <tr>
                <th>id</th>
                <th>姓名</th>
                <th>年龄</th>
                <th>性别</th>
                <th>操作</th>
            </tr>
        <% for( const item of arr){ %>
            <tr>
                <td><%= item.id %></td>
                <td><%= item.name %></td>
                <td><%= item.age %></td>
                <td><%= item.sex %></td>
                <td>
                    <a href="/deluser/<%= item.id %>">删除</a>
                    <a href="/edituser/<%= item.id %>">修改</a>
                </td>
            </tr>
        <%  }%>
    </table>
<hr>
    <form action="/adduser" method="post">
       姓名：<input type="text" name="name"><br>
       年龄：<input type="number" name="age" min="0" max="150"><br>
       性别：<input type="radio" name="sex" value="男">男
            <input type="radio" name="sex" value="女">女<br>
        <button>提交</button>
        </form>
</body>
</html>